schema {
    query: Query
    mutation: Mutation
}
type Query {
    me: User
#    products(key: String): ProductsConnection!
#    orders(key: String): OrdersConnection!
#    merchant(id: ID!): Merchant
}
type Mutation {
    createUser(email: String!, password: String!): User
}